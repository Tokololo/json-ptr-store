import { IStoreFlags, IStorePtr, IStoreValue, Store } from './store';
import { strictnessType, customStrictnessComparerType } from './library';
export { IStoreFlags, IStorePtr, IStoreValue, Store, strictnessType, customStrictnessComparerType }